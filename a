# Connect wifi first
nmcli dev wifi connect "0" password "salahbedairr"

# Install yay if not there
if ! command -v yay &>/dev/null; then
  tmpdir=$(mktemp -d)
  git clone https://aur.archlinux.org/yay.git "$tmpdir/yay"
  cd "$tmpdir/yay"
  makepkg -si --noconfirm
fi

# Install one by one
yay -S tuigreet --noconfirm
